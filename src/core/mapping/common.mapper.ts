// src/core/mapping/common.mapper.ts

/**
 * Placeholder for common mapping logic shared across providers.
 * Currently unused, specific mappers handle transformations.
 */

export {} // Ensure this file is treated as a module.
